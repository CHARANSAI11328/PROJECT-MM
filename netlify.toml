[build]
  publish = "."

# Proxy all /api/* requests to backend server when hosted on Netlify
[[redirects]]
  from = "/api/*"
  to = "https://mameka-backend.onrender.com/api/:splat"
  status = 200
  force = true
